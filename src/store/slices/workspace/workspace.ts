// import { AppNavigationSlice, AppWorkspace } from '../../core.ts'
// const slice = await AppNavigationSlice`default`.current_addr.where(AppWorkspace).ref.addr.one.reactive

export const workspaceSlice = {}
